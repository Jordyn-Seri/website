

Change the auto-rotate interval in `TestimonialsSection.tsx` from 6000ms to 18000ms.

**File: `src/components/TestimonialsSection.tsx`**
- Line ~46: Change `6000` to `18000` in the `setInterval` call.

